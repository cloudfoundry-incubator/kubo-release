---
name: kubernetes-proxy

templates:
  bin/kubernetes_proxy_ctl.erb: bin/kubernetes_proxy_ctl
  bin/pre-start: bin/pre-start
  config/syslog_forwarder.conf.erb: config/syslog_forwarder.conf
  config/kubernetes_proxy_syslog.conf: config/kubernetes_proxy_syslog.conf

packages:
- pid_utils
- kubernetes
- syslog_aggregator

properties:
  kubernetes-api-url:
    description: The full URL for the load balancer in front of the masters
    example: https://api.lite.helmsman.cf-app.com:8443
  syslog_aggregator.address:
    description: "Syslog drain hostname"
  syslog_aggregator.port:
    description: "Syslog drain port"
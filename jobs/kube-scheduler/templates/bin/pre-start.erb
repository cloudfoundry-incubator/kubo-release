#!/bin/bash

set -eou pipefail

check_file_existence(){
  file=$1

  echo "Checking file existence: $file"
  if [ ! -f "$file" ]; then
      echo "$file not exist"
      exit 1
  fi
}

main() {
  check_file_existence "/var/vcap/packages/kubernetes/bin/kube-scheduler"
}

main $@

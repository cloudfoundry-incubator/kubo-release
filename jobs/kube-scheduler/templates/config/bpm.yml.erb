---
processes:
- name: kube-scheduler
  executable: /var/vcap/packages/kubernetes/bin/kube-scheduler
  args:
  - --leader-elect
  - --kubeconfig
  - /var/vcap/jobs/kube-scheduler/config/kubeconfig
  - --v
  - <%= p('logging-level') %>

---
processes:
- name: kube-scheduler
  executable: /var/vcap/packages/kubernetes/bin/kube-scheduler
  args:
  - --v
  - <%= p('logging-level') %>
  - --config
  - /var/vcap/jobs/kube-scheduler/config/config.yml

---
name: kubernetes-api

templates:
  bin/kubernetes_api_ctl: bin/kubernetes_api_ctl
  config/tokens.csv.erb: config/tokens.csv
  config/authorization_policy.jsonl.erb: config/authorization_policy.jsonl

packages:
- pid_utils
- kubernetes

properties:
  users:
    description: List of user credentials
    example: [{username: a, password: b}, ...]

provides:
- name: etcd
  type: etcd
- name: kubernetes-api
  type: kubernetes-api
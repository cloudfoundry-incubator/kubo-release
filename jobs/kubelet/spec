---
name: kubelet

templates:
  bin/drain.erb: bin/drain
  bin/kubelet_ctl.erb: bin/kubelet_ctl
  bin/pre-start: bin/pre-start
  bin/post-start.erb: bin/post-start
  bin/ensure_kubelet_up_and_running.erb: bin/ensure_kubelet_up_and_running
  config/kubelet.pem.erb: config/kubelet.pem
  config/kubelet-key.pem.erb: config/kubelet-key.pem

packages:
- pid_utils
- kubernetes

properties:
  certs.kubernetes-ca.cert:
    description: The Kubernetes certificate authority
  certs.kubelet.cert:
    description: The kubelet certificate
  certs.kubelet.key:
    description: The kubelet certificate's private key
  kubernetes-api-url:
    description: The full URL for the load balancer in front of the masters
    example: https://api.lite.helmsman.cf-app.com:8443

